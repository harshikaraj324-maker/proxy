{
  "name": "mr-robot-proxy-worker",
  "version": "1.0.0",
  "description": "MR ROBOT — Cloudflare Worker proxy relay",
  "main": "worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
